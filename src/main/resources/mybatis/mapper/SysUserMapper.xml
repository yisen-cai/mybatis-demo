<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.glancebar.mybatis.mapper.SysUserMapper">
    <resultMap id="BaseResultMap" type="com.glancebar.mybatis.entity.SysUser">
        <id column="pk_id" jdbcType="BIGINT" property="pkId"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="email" jdbcType="VARCHAR" property="email"/>
        <result column="description" jdbcType="VARCHAR" property="description"/>
        <result column="avatar" jdbcType="VARCHAR" property="avatar"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="is_enabled" jdbcType="BIT" property="isEnabled"/>
        <result column="is_deleted" jdbcType="BIT" property="isDeleted"/>
    </resultMap>

    <!--  关联RoleMapper的ResultMap  -->
    <resultMap id="UserRoleResultMap" type="com.glancebar.mybatis.entity.SysUser" extends="BaseResultMap">
        <association property="role" columnPrefix="role_"
                     resultMap="com.glancebar.mybatis.mapper.SysRoleMapper.BaseResultMap"/>
    </resultMap>

    <resultMap id="UserRoleResultMapAnotherWay" type="com.glancebar.mybatis.entity.SysUser" extends="BaseResultMap">
        <association property="role" column="{pkId=role_id}"
                     fetchType="lazy"
                     select="com.glancebar.mybatis.mapper.SysRoleMapper.selectRoleById"/>
    </resultMap>

    <!-- One to One -->
    <select id="selectUserAndRoleByUserId" parameterType="long" resultMap="UserRoleResultMap">
        select u.pk_id,
               u.username,
               u.description,
               u.create_time,
               u.email,
               u.avatar,
               u.is_deleted,
               u.is_enabled,
               u.description,
               r.pk_id       role_pk_id,
               r.is_enabled  role_is_enabled,
               r.create_by   role_create_by,
               r.create_time role_create_time,
               r.role_name   role_role_name
        from sys_user u
                 inner join sys_user_role ur on u.pk_id = ur.user_id
                 inner join sys_role r on ur.role_id = r.pk_id
        where u.pk_id = #{userId}
    </select>


    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete
        from sys_user
        where pk_id = #{pkId,jdbcType=BIGINT}
    </delete>
    <insert id="insert" parameterType="com.glancebar.mybatis.entity.SysUser">
        <selectKey keyProperty="pkId" order="BEFORE" resultType="java.lang.Long">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into sys_user (pk_id, username, password,
        email, description, avatar,
        create_time, is_enabled, is_deleted
        )
        values (#{pkId,jdbcType=BIGINT}, #{username,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR},
        #{email,jdbcType=VARCHAR}, #{description,jdbcType=VARCHAR}, #{avatar,jdbcType=VARCHAR},
        #{createTime,jdbcType=TIMESTAMP}, #{isEnabled,jdbcType=BIT}, #{isDeleted,jdbcType=BIT}
        )
    </insert>
    <update id="updateByPrimaryKey" parameterType="com.glancebar.mybatis.entity.SysUser">
        update sys_user
        set username    = #{username,jdbcType=VARCHAR},
            password    = #{password,jdbcType=VARCHAR},
            email       = #{email,jdbcType=VARCHAR},
            description = #{description,jdbcType=VARCHAR},
            avatar      = #{avatar,jdbcType=VARCHAR},
            create_time = #{createTime,jdbcType=TIMESTAMP},
            is_enabled  = #{isEnabled,jdbcType=BIT},
            is_deleted  = #{isDeleted,jdbcType=BIT}
        where pk_id = #{pkId,jdbcType=BIGINT}
    </update>
    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select pk_id,
               username,
               password,
               email,
               description,
               avatar,
               create_time,
               is_enabled,
               is_deleted
        from sys_user
        where pk_id = #{pkId,jdbcType=BIGINT}
    </select>
    <select id="selectAll" resultMap="BaseResultMap">
        select pk_id,
               username,
               password,
               email,
               description,
               avatar,
               create_time,
               is_enabled,
               is_deleted
        from sys_user
    </select>
    <select id="selectByUserIdAndIsEnabled" resultType="com.glancebar.mybatis.entity.SysUser">
        select pk_id,
               username,
               password,
               email,
               description,
               avatar,
               create_time,
               is_enabled,
               is_deleted
        from sys_user
        where pk_id = #{userId}
          and is_enabled = #{isEnabled}
    </select>

    <select id="selectUserAndRoleByUserIdInAnotherWay" resultMap="UserRoleResultMapAnotherWay">
        select u.pk_id,
               u.username,
               u.description,
               u.create_time,
               u.email,
               u.avatar,
               u.is_deleted,
               u.is_enabled,
               u.description,
               ur.role_id role_id
        from sys_user u
                 inner join sys_user_role ur on u.pk_id = ur.user_id
        where u.pk_id = #{userId}
    </select>
</mapper>